version: "3.9"

services:
  jupyter:
    build: ./docker/jupyter
    container_name: dl_notebook_cpu
    ports:
      - "8888:8888"
    volumes:
      - ./notebooks:/home/jovyan/work
    environment:
      - JUPYTER_ENABLE_LAB=yes
    command: "start-notebook.sh --NotebookApp.token="
